[package]
name = "noter-watch-engine"
description = "Phase 1.75 — Workspace Watch Engine: notify-based file watcher + EventFilter + ChangeQueue debounce + IncrementalIndexer integration. Runs in background thread, exposes drain_events() for NAPI polling."
version.workspace = true
edition.workspace = true

[dependencies]
anyhow.workspace            = true
serde.workspace             = true
serde_json.workspace        = true
tracing.workspace           = true
crossbeam-channel.workspace = true
noter-watcher               = { path = "../noter-watcher" }
noter-index-engine          = { path = "../noter-index-engine" }
